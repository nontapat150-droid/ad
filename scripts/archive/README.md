# Archived one-off scripts

Scripts moved here are **not used at runtime**. They were kept for reference only.

- `root/` — ad-hoc migration/fix scripts from project root
- `backend/` — one-time test and debug scripts from `backend/`

Do not import these from the application. Use `/api/migrate/migrate-fix` for schema changes.
